spring:
  application:
    name: rabbitmq-playground
  rabbitmq:
    addresses: ${CLOUDAMQP_URL:amqp://admin:admin@localhost:5673}

server:
  port: ${PORT:8099}

app:
  cors:
    allowed-origins: ${CORS_ORIGINS:http://localhost:5173,http://localhost:5174}
